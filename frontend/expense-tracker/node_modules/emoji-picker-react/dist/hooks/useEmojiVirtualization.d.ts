import { ReactNode } from 'react';
import { DataEmojis } from '../dataUtils/DataTypes';
export declare function useEmojiVirtualization({ categoryEmojis, topOffset, onHeightReady, scrollTop }: {
    categoryEmojis: DataEmojis;
    topOffset: number;
    onHeightReady: (height: number) => void;
    scrollTop: number;
}): {
    virtualizedCounter: number;
    emojis: ReactNode[];
    dimensions: {
        categoryHeight: number;
        emojisPerRow: number;
        emojiSize: number;
    } | undefined;
};
